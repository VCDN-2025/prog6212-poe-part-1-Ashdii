<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Submit New Claim - CMCSystem</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #3498db;
            --accent-color: #e74c3c;
            --light-color: #ecf0f1;
            --dark-color: #2c3e50;
            --success-color: #2ecc71;
            --warning-color: #f39c12;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            flex: 1;
            max-width: 800px;
            margin: 0 auto;
        }

        /* Header styling */
        .page-header {
            color: var(--primary-color);
            font-weight: 700;
            margin-bottom: 2rem;
            text-align: center;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--secondary-color);
        }

        /* Form styling */
        .claim-form {
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
            padding: 2.5rem;
            margin-bottom: 2rem;
        }

        .form-label {
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .form-control, .form-select {
            border: 2px solid #e2e8f0;
            border-radius: 8px;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
        }

            .form-control:focus, .form-select:focus {
                border-color: var(--secondary-color);
                box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
            }

        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }

        /* File upload area */
        .file-upload {
            background-color: #f8f9fa;
            border: 2px dashed #dee2e6;
            border-radius: 8px;
            padding: 1.5rem;
            text-align: center;
            margin-top: 0.5rem;
            transition: all 0.3s ease;
        }

            .file-upload:hover {
                border-color: var(--secondary-color);
                background-color: #e8f4fe;
            }

        /* Buttons */
        .btn-primary {
            background: linear-gradient(to right, var(--secondary-color), #2980b9);
            border: none;
            border-radius: 8px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            box-shadow: 0 4px 8px rgba(52, 152, 219, 0.3);
            transition: all 0.3s ease;
            min-width: 140px;
        }

            .btn-primary:hover {
                background: linear-gradient(to right, #2980b9, var(--secondary-color));
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(52, 152, 219, 0.4);
            }

        .btn-secondary {
            background: linear-gradient(to right, #6c757d, #5a6268);
            border: none;
            border-radius: 8px;
            padding: 0.75rem 1.5rem;
            font-weight: 600;
            box-shadow: 0 4px 8px rgba(108, 117, 125, 0.3);
            transition: all 0.3s ease;
            min-width: 140px;
        }

            .btn-secondary:hover {
                background: linear-gradient(to right, #5a6268, #6c757d);
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(108, 117, 125, 0.4);
            }

        .form-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }



        {
            padding: 1.5rem;
        }

        .form-actions {
            flex-direction: column;
        }

        .btn-primary, .btn-secondary {
            width: 100%;
        }

        }


       

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .animated {
            animation: fadeIn 0.8s ease forwards;
        }

        .info-card {
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="page-header animated">Submit New Claim</h1>

        <div class="claim-form animated">
            <form method="post" enctype="multipart/form-data">
                <div class="mb-4">
                    <label class="form-label">Passenger Name</label>
                    <input type="text" class="form-control" placeholder="Enter passenger's full name" />
                </div>

                <div class="mb-4">
                    <label class="form-label">Claim Type</label>
                    <select class="form-select">
                        <option selected disabled>Select claim type</option>
                        <option>Lost Baggage</option>
                        <option>Flight Delay</option>
                        <option>Damaged Item</option>
                        <option>Cancelled Flight</option>
                        <option>Other</option>
                    </select>
                </div>

                <div class="mb-4">
                    <label class="form-label">Description</label>
                    <textarea class="form-control" placeholder="Please provide a detailed description of your claim"></textarea>
                    <div class="form-text">Be as specific as possible to help us process your claim faster.</div>
                </div>

                <div class="mb-4">
                    <label class="form-label">Upload Supporting Documents</label>
                    <div class="file-upload">
                        <i class="fas fa-cloud-upload-alt fa-2x mb-3 text-secondary"></i>
                        <p class="mb-2">Drag & drop files here or click to browse</p>
                        <p class="small text-muted">Maximum file size: 10MB (dummy only)</p>
                        <input type="file" class="d-none" disabled />
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-paper-plane me-2"></i>Submit Claim
                    </button>
                    <a href="/Claim" class="btn btn-secondary">
                        <i class="fas fa-times me-2"></i>Cancel
                    </a>
                </div>
            </form>
        </div>

        <div class="info-card animated">
            <div class="card border-0 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-info-circle me-2 text-primary"></i>Need Help?</h5>
                    <p class="card-text">If you need assistance with your claim, please contact our support team:</p>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-phone me-2 text-success"></i>+1 (800) 123-4567</li>
                        <li><i class="fas fa-envelope me-2 text-warning"></i>support@cmcsystem.com</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Simple animation for form elements
        document.addEventListener('DOMContentLoaded', function() {
            const formElements = document.querySelectorAll('.form-control, .form-select, .file-upload');
            formElements.forEach((element, index) => {
                element.style.animationDelay = `${index * 0.1}s`;
            });
        });
    </script>
</body>
</html>